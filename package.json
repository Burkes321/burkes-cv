{
  "name": "samuel-burke-cv",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i styles.css -o dist/output.css --minify",
    "dev": "tailwindcss -i styles.css -o dist/output.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
